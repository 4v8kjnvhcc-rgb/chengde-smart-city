---
description: 项目结束前不再分期 P0/P1，按模块最终目标一次做全
alwaysApply: true
---

# 交付节奏（硬约束 · 2026-07-25 起）

自用户声明之日起至项目结束：

1. **禁止**再以「先做 P0、P1 以后再说」拆分收工或写入迭代记录作为未完成出口。
2. 原 D20 中的 P0/P1/P2 **只描述完成度维度**（底线 / 友好可用 / 运维），不是分期排期。
3. 每个模块改造须一次做到**该模块最终目标**：真实数据流、登记对象选择、中文状态、可监控/可回看、无 mock 假成功、与黄金路径/既定引擎衔接等（见 D20 全文）。
4. 文档与对话中避免「本轮只做 P0」；若范围过大，应划清**模块边界**后仍对该边界内目标做全，而不是降级为演示版。
5. 更新 D17 时写清已完成项；未完成项须标明「本轮未做原因（模块边界外）」而非「留作 P1」。

## 对话与方案表述（强制）

- **禁止**在回复用户时用「P0 包 / P1 包 / 先做 A 后做 B 分期」拆交付方案；应直接给出**该模块最终版**工作清单并一次落地。
- 用户纠正「没有 P0/P1」时，立即按最终版执行，并同步 OpenViking `remember`（若用户要求写记忆）。
- 「底线 / 友好可用 / 运维」仅作自检维度，不得改成排期标签。
