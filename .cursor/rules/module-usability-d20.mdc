---
description: 模块实用化与交互验收（D20）——后续模块改造硬标准
alwaysApply: true
---

# 模块实用化标准（D20）

完整条文见 [`D20-模块实用化与交互验收标准.md`](D20-模块实用化与交互验收标准.md)。

## 完成度维度（不是分期）

1. **底线**：D02 / D05 / V3.0 功能清单（能力必须真跑通）
2. **友好可用**：少手填、中文、可解释、无死胡同
3. **运维增强**：监控、日志、幂等等

**命名先行**：Hub 侧栏/页标题须对齐 V3.0 目录名，子能力先映射已实现框架（见 `v3-module-naming.mdc`），再按本规则做实用化。

**分层口径**：过程数据（治理/DWD）≠ 目录资源；融合多表成主题/专题（DWS/ADS）后经质量与编目才进门户；直通源资源亦可编目（见 `data-layer-process-vs-resource.mdc` / D07 §9.3.1）。

**交付节奏**：至项目结束不再分 P0/P1 分期收工；每一轮按模块**最终目标一次做全**（见 `full-delivery-no-p0p1.mdc`）。

## 硬约束（改任何业务模块前自检）

- 控制面 `smart_city` vs 数据面 ODS/DWD/DWS/ADS；后续模块只选已登记对象，禁止重复填 JDBC
- 治理/搬运类任务：**只用既定引擎（Kettle 等）**，禁止再做平行「内存版治理」
- 「查看结果」读**目标落层**（治理后看 DWD/DWS/ADS），不得默默读 ODS/源表
- 占位默认（`table_name`、`output_table`、`/tmp/...`）不得未校验即执行
- 类型不安全变换（如 MD5→DATE）必须新列或拦截
- 状态展示 `statusLabel`；工具栏 `portal-inline-form`；加载按当前视图、并行≤3
- `sys_admin` 全量放行
- 交付前勾 D20 §四 清单，并更新 D17 迭代记录
