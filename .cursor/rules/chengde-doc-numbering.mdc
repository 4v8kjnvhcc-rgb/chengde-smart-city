---
description: 承德项目工程 Markdown 文档编号与命名规范（D00～Dnn）
globs: D*.md
alwaysApply: false
---

# 工程文档编号规范

编制或修改本项目 `D*.md` 工程文档时须遵守：

1. **文件名**：`D{nn}-{文档简称}.md`（两位序号，如 `D08-验收测试用例.md`）
2. **正文元数据**：属性表首行含 `| **文档编号** | **D{nn}** |`
3. **登记**：在 `D00-文档索引与编号规范.md` 总表新增一行（编号、文件名、版本、状态）
4. **交叉引用**：链接使用新文件名，如 `[D02-需求基线说明.md](D02-需求基线说明.md)`
5. **流程顺序**：编号按工程文档链递增；已占用序号不得复用
6. **版本号**：独立于文档编号（如 D02 V2.2、D07 V1.1）

当前链：D01 评估 → D02 基线 → D03 ESB → D04 选型 → D05 清单 → D06 映射 → D07 架构 → D08 验收（P0）→ D09 等保；D10 预留 OM 菜单映射。

甲方 Word、厂商 docx、`_doc_extract.txt` 等原始素材不使用 D 编号。
