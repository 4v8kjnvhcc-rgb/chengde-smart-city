# 只打包本机已构建好的 dist（构建过程见 scripts/build_prod_images.ps1）
# 用途：本机 Docker 容器内 DNS 常不可用，故在宿主机跑 npm build，再把产物打进镜像
FROM nginx:1.27-alpine
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
# 与 vite base=/bigdata-web/、门户入口 /bigdata-web 对齐
COPY dist /usr/share/nginx/html/bigdata-web
EXPOSE 80
