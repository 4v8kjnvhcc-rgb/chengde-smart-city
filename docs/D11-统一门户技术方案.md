# 统一门户技术方案（D11 V0.6 — MS1）

| 属性 | 说明 |
|------|------|
| **文档编号** | **D11** |
| **文档版本** | V0.6（与 `platform-web` 现行实现对齐） |
| **编制日期** | 2026-06-24 |
| **文档性质** | MS1 门户壳、路由/RBAC 契约 + 政务蓝视觉规范 |
| **配套** | [`D07-总体技术架构设计.md`](D07-总体技术架构设计.md) §5.6、§5.8；[`D06-Mxxx实现映射表.md`](D06-Mxxx实现映射表.md)、[`D10-OM菜单名映射表.md`](D10-OM菜单名映射表.md) |

---

## 一、MS1 范围

| 项 | 说明 |
|----|------|
| **已实现页面** | 登录、统一门户 Hub（`/dashboard`）、用户/角色/机构/菜单、审计、等保开关 |
| **占位页面** | D07 §5.6 其余业务菜单（`integration_type=placeholder/om/iframe/esb`）→ `PlaceholderView` |
| **路由前缀** | `/dashboard`、`/system/*`、业务域按 D07 一级路径 |
| **菜单数据源** | `sys_menu` + Flyway V3 seed；可再生脚本 `scripts/gen_portal_menu_seed.py` |
| **本地开发** | `platform-web` Vite 端口 **3000**（`vite.config.ts`）；API 代理至 `localhost:8080` |

## 二、路由约定

| 类型 | 路径模式 | 前端组件 | 后端集成 |
|------|----------|----------|----------|
| 登录 | `/login` | `views/LoginView.vue` | `/api/v1/auth/login` |
| 门户 Hub | `/dashboard` | `views/DashboardView.vue` | — |
| 自研 | `/system/users` 等 | `views/system/*` | `/api/v1/system` |
| 占位 | `/exchange/*`、`/governance/*` 等 | `PlaceholderView`（catch-all） | MS2+ 实现 |
| OM | `/governance/*` | MS3 代理壳 | `/api/v1/integration/om` |
| iframe | `/analytics/*` | MS6 DataEase | `/api/v1/integration/dataease` |
| ESB | `/exchange/esb` | MS2 深链 | M214 |

**路由守卫**（`router/index.ts`）：

- 未登录访问业务路由 → 重定向 `/login`
- 已登录访问 `/login` → 重定向 `/dashboard`
- 已登录但 `menus`/`permissions` 为空 → `fetchProfile()`
- `afterEach`：浏览器标题 `{meta.title} · 承德智慧城市`

## 三、RBAC

- 菜单树与 D07 §5.6 结构一致（TC-M211-002）
- 按钮权限码：`system:user:query|add|edit|delete` 等，存 `sys_menu.menu_type=3`
- 前端：`stores/auth.permissions` + 按钮级 `hasPermission`（如用户管理「新增」）
- 机构隔离：非 `SYSTEM_ADMIN` 仅本机构用户（`UserService.assertOrgAccess`）
- **Hub 卡片**：`getAuthorizedPlatforms` — 无权限或平台下无子菜单则不显示该卡

## 四、菜单 seed 再生

```bash
python scripts/gen_portal_menu_seed.py --check
```

输出：`menu/menu-seed.json`、`db/migration/V3__seed_ms1_menu_and_users.sql`

## 五、布局结构（单 MainLayout，按路由切换模式）

```
App.vue（根 <router-view />）
├── /login          → LoginView（无 Layout）
└── MainLayout
    ├── /dashboard（Hub 模式）
    │   ├── 无侧栏
    │   ├── AppHeader（hubTheme：透明顶栏，无「返回总览」）
    │   └── DashboardView（窄柱抽屉卡片 + Hub 背景层）
    ├── /system/*（系统管理）
    │   ├── AppSidebar（平台级菜单：用户/角色/机构…）
    │   ├── AppHeader（showBackHub + 白底顶栏）
    │   └── 业务页
    └── 其他业务子系统
        ├── 子系统有下级菜单 → AppSidebar（仅当前子系统菜单）
        ├── 叶子子系统（MS1 多数）→ 无侧栏，顶栏 + 内容
        └── AppHeader（showBackHub）+ PlaceholderView / 业务页
```

侧栏数据由 `utils/menu.ts` 的 `resolveSidebarContext` 计算，**非**手写路由表。

## 六、视觉与布局规范（政务蓝）

### 6.1 色板

| 用途 | 色值 | CSS 变量 |
|------|------|----------|
| 主色 | `#1677ff` | `--portal-primary` |
| 主色深 | `#1a5fb4` | `--portal-primary-dark` |
| 侧栏背景 | `#001529` | `--portal-sidebar-bg` |
| 顶栏背景 | `#ffffff` | `--portal-header-bg` |
| 页面背景 | `#f0f2f5` | `--portal-bg` |
| 卡片背景 | `#ffffff` | `--portal-card-bg` |
| 正文 | `#1f2937` | `--portal-text` |
| 次要文字 | `#6b7280` | `--portal-text-secondary` |
| 边框 | `#e5e7eb` | `--portal-border` |

样式链：`variables.css` → `global.css` → `element-overrides.css` → `style.css`（`main.ts` 引入）。

### 6.2 字体与间距

| 项 | 约定 |
|----|------|
| 字体栈 | `PingFang SC`、`Microsoft YaHei`、`system-ui` |
| 顶栏高度 | `56px`（`--portal-header-height`） |
| 侧栏宽度 | `220px` |
| 内容区内边距 | `20px` |
| 卡片圆角 | `8px`（`--portal-radius`） |

### 6.3 登录页

- 整页 **105° 蓝白渐变**（深蓝 `#001529` → 右侧约 15% 浅区 `#f5f8fc`）；非左右硬切双色块
- 桌面：左侧品牌文案 + 右侧半透明白卡片表单（`label-position="top`，主按钮全宽）
- 支持双因素输入框（等保开关开启时使用）
- 登录成功 → `/dashboard`

## 七、通用组件清单

| 组件 | 路径 | 用途 |
|------|------|------|
| `PageHeader` | `components/common/PageHeader.vue` | 页面标题、描述、右侧 `slot` |
| `PageCard` | `components/common/PageCard.vue` | 白卡片容器 |
| `EmptyState` | `components/common/EmptyState.vue` | 占位/建设中 |
| `AppHeader` | `components/layout/AppHeader.vue` | 顶栏；`hubTheme` / `showBackHub`；Logo 点击回 Hub |
| `AppSidebar` | `components/layout/AppSidebar.vue` | 动态侧栏（`router` 模式 `el-menu`） |
| `SidebarMenuItem` | `components/layout/SidebarMenuItem.vue` | 递归菜单项（过滤 `menuType=3`） |

业务页：`PageHeader` + `PageCard`；占位路由：`EmptyState`。

## 八、门户首页（Hub · 抽屉式平台卡片）

依据 **P17**（WorkBuddy：`P17-门户首页抽屉式卡片设计规格书.md`），MS1 实现为 **窄柱卡片** 形态（与 P17 初版「四卡等宽铺满」略有差异，以本方案与代码为准）。

| 项 | 现行实现（`DashboardView.vue`） |
|----|--------------------------------|
| **路由** | `/dashboard`，`meta.title` =「统一门户」 |
| **主标题** | 「承德高新区智慧城市数据中台」（28px，居中） |
| **卡片布局** | 四平台 **148px 宽** 窄柱，横向居中排列，`gap: 16px`；收起高度 **168px** |
| **Hub 背景** | `MainLayout` 在 Hub 模式叠加 `/images/hub-tech-bg.jpg`（`opacity: 0.3`）；`public/images/` 另备 `hub-tech-bg.svg` 可作替换素材 |
| **顶栏** | `AppHeader` 透明背景（`hubTheme`），无面包屑、无「返回总览」 |
| **交互** | 点击卡片 header 展开子系统抽屉；互斥展开；再次点击收起 |
| **聚焦** | 非激活卡 `opacity: 0.45`（现行未使用 `scale`） |
| **子系统** | `getAuthorizedPlatforms` → `children` 列表；点击 `firstNavPath` 跳转 |
| **无权限** | 无可用平台 → `el-empty` |
| **主题色** | 四平台独立配色（蓝/绿/橙/紫），组件内 `--card-*` CSS 变量 |
| **响应式** | `≤768px` 卡片纵向堆叠 |

### 8.1 导航层级（门户 → 子系统）

| 层级 | 路由示例 | 侧栏 |
|------|----------|------|
| 门户 Hub | `/dashboard` | 无 |
| 业务子系统 | `/exchange/ingestion`、`/governance` 等 | **仅当前子系统**下级菜单；无子菜单则不显示侧栏 |
| 系统管理 | `/system/users` 等 | **平台级**侧栏（用户/角色/机构等并列） |

门户抽屉点子系统 → 直达该子系统路由；**非系统管理**不再展示平台级同级子系统列表。

### 8.2 菜单工具（`utils/menu.ts`）

| 函数 | 说明 |
|------|------|
| `getAuthorizedPlatforms` | Hub 四卡数据源（有权限且有子菜单） |
| `firstNavPath` | 进入子系统默认路由 |
| `resolveSidebarContext` | 计算侧栏 `{ title, menus }` 或 `null` |
| `findSubsystemRoot` | 业务路由 → 子系统节点 |
| `isSystemRoute` | `/system` 前缀判断 |
| `PLATFORM_ROUTE_MAP` | 平台路径映射（含 `/governance` 等归主数据域） |

## 九、修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| V0.1 | 2026-06-18 | MS1 脚手架：门户壳 + 系统管理 + 占位路由 |
| V0.2 | 2026-06-23 | 政务蓝视觉规范；Layout 拆分；通用组件；用户新增/角色菜单授权 UI |
| V0.3 | 2026-06-23 | P17 抽屉式门户首页；子系统列表展开与跳转 |
| V0.4 | 2026-06-23 | 业务域子系统侧栏；系统管理保持平台级侧栏 |
| V0.5 | 2026-06-24 | Hub 窄柱卡片与背景层（草案） |
| V0.6 | 2026-06-24 | **按 `platform-web` 现行代码全文校对**（路由守卫、Hub 尺寸、背景路径、登录渐变、侧栏逻辑） |
