2026.9.3
1.图一："用于Oauth2服务认证的clientid信息"改成“client_id”，“用于Oauth2服务认证的client secret信息”改成“client_secret”，两个的顺序换一下；调整表格宽度，“接口信息”左侧一列的宽度宽一些，基本信息的左侧一列的宽度窄一些，两个表格宽度保持一致；
   → 已完成（PortalHomeView；CatalogSubscriptionView 同步文案/顺序）
2.图二：在规范设计、指标与目录体系构建之间新插入一个菜单“部门数据管理”，下面有两个子菜单，分别是“模板管理”、“上传记录”，其中“模板管理”的功能和本地文件上传的上传模板tab页的功能一致，“上传记录”和本地文件上传的最近上传tab页的功能一致。这两边的数据用的是同一套数据，只是本地文件上传是平台管理员和超级管理员用的，是可以管理所有组织机构的模板的和上传的；而新增的“部门数据管理”是部门自己用的，只能管理本部门的模板和上传，不能管理和查看其他部门的模板和上传；新增的“部门数据管理”及下级菜单“模板管理”、“上传记录”，添加到统一用户管理系统的菜单管理中，并把这几个功能添加到超级管理员、部门管理员的角色权限中。
   → 已完成（侧栏+复用 ManualUploadView；Flyway V255 + sql/patch/20260903_ingestion_dept_data_menus.sql）

3.图一：红框中括号中的内容及括号去掉；db_schema(SCHEMA)整行去掉；db_name改成必填；端口号直接填，不用上下箭头；db_host的文本框的提示词去掉；
4.图二：红框里的文字去掉；

5.图一：去掉是否统计项和是否排序项
6.图二、图三：提交改成确定；
7.图四：更新周期显示字典值；申请人去掉；
8.图五：显示中文名称；
9.图六：去掉红框中中的提示词，黄框中期限不能超过3656天改成期限不能超过365天，并加上校验，超过365填弹框提示，且不能提交，服务接口调用频次和服务接口峰值频率也加上正整数的校验；

10.图一：去掉黄框中的文字；红框中的文字改成“无”
11.图二：去掉红框中中的提示词，黄框中期限不能超过3656天改成期限不能超过365天，并加上校验，超过365填弹框提示，且不能提交，服务接口调用频次和服务接口峰值频率也加上正整数的校验；

12.图一和图二涉及审批的模块，手机号改成非必填，如果选择通过，意见中默认显示“同意”，其他模块涉及审核时也同理修改；

13.图一“前往数据项管理”改成“确定”

2026.9.4
1.图一：1.去掉红框里的内容；2.“服务请求方法”改成“请求方式”；3.接口请求路径下方内容改成“资源申请通过后前往个人中心查看”，接口目标地址同理；4.请求参数和响应参数的列表中，顺序为参数名称、参数描述、数据类型、是否必填；
2.图二：1.库表信息、接口改成“详细信息”；2.另外两个竖形红框里的内容去掉，右侧框宽度调整为与基本信息下方表格的宽度相同；
3.个人空间改成个人中心；
4.图三：去掉“目录code”；去掉"数据项摘要"；去掉字段长度列和数据敏感级别列；

5.图一：已挂载目录改成可以多选；





