# Linux 服务器上执行的脚本 / 生产 env 必须用 LF，避免 bash\r、grep 端口带 \r
*.sh text eol=lf
compose/*.env text eol=lf
compose/*.env.example text eol=lf

# Windows PowerShell 5.1 脚本建议保留 CRLF；文件本身须带 UTF-8 BOM（见 D23 V4.5）
scripts/*.ps1 text eol=crlf
scripts/*.bat text eol=crlf
